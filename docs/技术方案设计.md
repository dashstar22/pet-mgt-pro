# 宠物领养平台 — 技术方案设计

## 一、项目概述

### 1.1 项目名称
宠物领养管理平台 (Pet Adoption Management Platform)

### 1.2 技术架构
前后端分离架构：Spring Boot 3.5.6（后端 REST API）+ Vue 3（前端 SPA）

### 1.3 设计目标
- 将现有 Thymeleaf 服务端渲染应用改造为前后端分离架构
- 后端提供 RESTful API，JSON 格式数据交互
- 前端 Vue3 SPA 提供现代化用户界面
- JWT 无状态认证替代 Session 登录

---

## 二、后端技术方案

### 2.1 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 3.5.6 | 应用框架 |
| MyBatis-Plus | 3.5.9 | ORM / 数据访问 |
| Spring Security | 6.x | 安全框架 |
| JWT (jjwt) | 0.12.x | Token 生成与验证 |
| MySQL | 8.0 | 数据库 |
| Maven | 3.6+ | 构建管理 |
| JDK | 17+ | 运行环境 |

### 2.2 项目结构

```
src/main/java/com/petmgt/
├── PetMgtApplication.java              # 启动类
├── config/
│   ├── SecurityConfig.java             # Spring Security配置（改造）
│   ├── JwtConfig.java                  # JWT配置（新增）
│   ├── CorsConfig.java                 # CORS配置（新增）
│   ├── MyBatisPlusConfig.java          # MyBatis-Plus配置（保留）
│   ├── FileUploadConfig.java           # 文件上传配置（保留）
│   ├── MvcConfig.java                  # MVC配置（改造）
│   └── AiConfig.java                   # AI配置（保留）
├── security/
│   ├── JwtTokenProvider.java           # JWT生成/验证工具（新增）
│   ├── JwtAuthenticationFilter.java    # JWT认证过滤器（新增）
│   └── JwtAuthenticationEntryPoint.java # 认证入口点（新增）
├── controller/
│   ├── AuthController.java             # 认证接口（改造）
│   ├── PetController.java              # 宠物公开接口（改造）
│   ├── BreedController.java            # 品种公开接口（改造）
│   ├── FileController.java             # 文件上传接口（新增）
│   ├── user/
│   │   ├── ProfileController.java      # 用户个人信息（改造）
│   │   ├── ApplicationController.java  # 用户申请（改造）
│   │   ├── AiMatchController.java      # AI匹配（改造）
│   │   └── AiChatController.java       # AI问答（改造）
│   └── admin/
│       ├── AdminController.java        # 管理后台统计（改造）
│       ├── UserController.java         # 用户管理（改造）
│       ├── PetManageController.java    # 宠物管理（改造）
│       ├── BreedController.java        # 品种管理（改造）
│       ├── ApplicationController.java  # 申请审核（改造）
│       └── AiRecordController.java     # AI记录（改造）
├── dto/
│   ├── LoginRequest.java               # 登录请求（新增）
│   ├── LoginResponse.java              # 登录响应（新增）
│   ├── RegisterForm.java               # 注册表单（保留）
│   ├── ApplicationForm.java            # 申请表单（保留）
│   ├── PetSearchCriteria.java          # 搜索条件（保留）
│   ├── AiMatchRequest.java             # AI匹配请求（保留）
│   ├── AiMatchResult.java              # AI匹配结果（保留）
│   ├── AiReviewResult.java             # AI审核结果（保留）
│   └── ApiResponse.java                # 统一API响应（新增）
├── entity/                             # 实体类（保留）
├── service/                            # 业务逻辑（保留，微调）
├── mapper/                             # 数据访问（保留）
├── exception/
│   └── BusinessException.java          # 业务异常（保留）
├── handler/
│   └── GlobalExceptionHandler.java     # 全局异常处理（改造）
└── util/
    ├── SecurityUtil.java               # 安全工具（改造）
    └── FileUtil.java                   # 文件工具（保留）
```

### 2.3 统一响应格式

```java
public class ApiResponse<T> {
    private int code;       // 200 成功, 400 参数错误, 401 未登录, 403 无权限, 500 服务器错误
    private String msg;     // 提示消息
    private T data;         // 数据体

    public static <T> ApiResponse<T> success(T data) { ... }
    public static <T> ApiResponse<T> error(int code, String msg) { ... }
}
```

分页响应：
```java
public class PageResponse<T> {
    private List<T> records;
    private long total;
    private int page;
    private int size;
}
```

### 2.4 API 接口设计

#### 2.4.1 用户认证（AuthController）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/auth/register | 用户注册 | 公开 |
| POST | /api/auth/login | 用户登录，返回 JWT Token | 公开 |
| GET | /api/user/profile | 获取当前用户信息 | 登录 |
| PUT | /api/user/profile | 更新个人信息 | 登录 |
| PUT | /api/user/password | 修改密码 | 登录 |

**POST /api/auth/login 请求体：**
```json
{ "username": "string", "password": "string" }
```

**POST /api/auth/login 响应：**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "username": "admin",
    "roles": ["ROLE_ADMIN"]
  }
}
```

#### 2.4.2 宠物信息（PetController）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/pets | 宠物列表(分页+筛选) | 公开 |
| GET | /api/pets/{id} | 宠物详情 | 公开 |
| POST | /api/pets | 发布宠物 | 管理员 |
| PUT | /api/pets/{id} | 编辑宠物 | 管理员 |
| DELETE | /api/pets/{id} | 删除宠物 | 管理员 |

**GET /api/pets 参数：** `?page=1&size=12&name=xx&breedId=1&status=available`

**GET /api/pets 响应：**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "小白",
        "breedId": 1,
        "breedName": "金毛",
        "gender": "公",
        "age": 12,
        "weight": 15.5,
        "healthStatus": "健康",
        "personality": "温顺亲人",
        "status": "available",
        "coverImageUrl": "uploads/pets/xxx.jpg",
        "createdAt": "2026-05-01T10:00:00"
      }
    ],
    "total": 25,
    "page": 1,
    "size": 12
  }
}
```

#### 2.4.3 品种管理（BreedController）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/breeds | 品种列表(可按petType筛选) | 公开 |
| POST | /api/breeds | 新增品种 | 管理员 |
| PUT | /api/breeds/{id} | 编辑品种 | 管理员 |
| DELETE | /api/breeds/{id} | 删除品种 | 管理员 |

#### 2.4.4 领养申请（ApplicationController）

**用户端：**

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/applications | 提交申请 | 登录 |
| GET | /api/applications | 我的申请列表 | 登录 |
| GET | /api/applications/{id} | 申请详情 | 登录 |
| PUT | /api/applications/{id}/cancel | 取消申请 | 登录 |
| DELETE | /api/applications/{id} | 删除记录 | 登录 |

**管理端：**

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/admin/applications | 审核列表 | 管理员 |
| GET | /api/admin/applications/{id} | 审核详情(含AI建议) | 管理员 |
| PUT | /api/admin/applications/{id}/approve | 通过 | 管理员 |
| PUT | /api/admin/applications/{id}/reject | 拒绝 | 管理员 |

#### 2.4.5 AI 功能

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/ai-match | AI 宠物匹配 | 登录 |
| GET | /api/ai-match/history | 匹配历史 | 登录 |
| POST | /api/ai-chat | AI 养宠问答 | 登录 |

#### 2.4.6 文件上传

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/upload | 上传文件（multipart/form-data） | 登录 |

**POST /api/upload 响应：**
```json
{ "code": 200, "msg": "success", "data": { "url": "uploads/pets/xxx.jpg" } }
```

#### 2.4.7 管理后台

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/admin/stats | 统计数据 | 管理员 |
| GET | /api/admin/users | 用户管理列表 | 管理员 |
| POST | /api/admin/users | 新增用户 | 管理员 |
| PUT | /api/admin/users/{id} | 编辑用户 | 管理员 |
| DELETE | /api/admin/users/{id} | 删除用户 | 管理员 |

### 2.5 JWT 认证流程

```
1. 用户 POST /api/auth/login { username, password }
2. 后端验证用户名密码 → 生成 JWT Token → 返回给前端
3. 前端保存 Token 到 localStorage
4. 后续请求在 Header 中携带: Authorization: Bearer <token>
5. JwtAuthenticationFilter 拦截请求 → 验证 Token → 设置 SecurityContext
6. Token 过期后，前端收到 401 → 跳转登录页
```

JWT Token 结构：
```json
{
  "sub": "admin",
  "userId": 1,
  "roles": ["ROLE_ADMIN"],
  "iat": 1717027200,
  "exp": 1717113600
}
```

### 2.6 安全配置

```java
// SecurityConfig 改造要点
- 关闭 formLogin() 和 httpBasic()
- 添加 .sessionManagement().sessionCreationPolicy(STATELESS)
- 添加 JwtAuthenticationFilter 到 UsernamePasswordAuthenticationFilter 之前
- 配置 .exceptionHandling() 返回 JSON 而非重定向
```

### 2.7 CORS 配置

```java
@Configuration
public class CorsConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/api/**")
            .allowedOrigins("http://localhost:5173")
            .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
            .allowedHeaders("*")
            .allowCredentials(true);
    }
}
```

### 2.8 全局异常处理

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(BusinessException.class)
    public ApiResponse<Void> handleBusiness(BusinessException e) {
        return ApiResponse.error(400, e.getMessage());
    }
    @ExceptionHandler(AccessDeniedException.class)
    public ApiResponse<Void> handleAccessDenied() {
        return ApiResponse.error(403, "无权限");
    }
    @ExceptionHandler(Exception.class)
    public ApiResponse<Void> handleException() {
        return ApiResponse.error(500, "服务器内部错误");
    }
}
```

---

## 三、前端技术方案

### 3.1 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.4+ | 前端框架 |
| Vite | 5.x | 构建工具 |
| Vue Router | 4.x | 路由管理 |
| Pinia | 2.x | 状态管理 |
| Element Plus | 2.x | UI组件库 |
| Axios | 1.x | HTTP客户端 |
| @vueuse/core | latest | 组合式工具函数 |

### 3.2 项目结构

```
frontend/
├── index.html
├── package.json
├── vite.config.js
└── src/
    ├── main.js                      # 入口
    ├── App.vue                      # 根组件
    ├── api/                         # API 封装
    │   ├── request.js               # Axios 实例 + 拦截器
    │   ├── auth.js                  # 认证 API
    │   ├── pets.js                  # 宠物/品种 API
    │   ├── applications.js          # 申请 API
    │   ├── ai.js                    # AI API
    │   └── admin.js                 # 管理后台 API
    ├── router/
    │   └── index.js                 # 路由配置 + 导航守卫
    ├── stores/
    │   ├── user.js                  # 用户状态 (token, userInfo, isAdmin)
    │   └── app.js                   # 全局状态 (breeds缓存)
    ├── views/
    │   ├── HomeView.vue
    │   ├── LoginView.vue
    │   ├── RegisterView.vue
    │   ├── PetListView.vue
    │   ├── PetDetailView.vue
    │   ├── NotFoundView.vue         # 404 页面
    │   ├── user/
    │   │   ├── ProfileView.vue
    │   │   ├── MyApplicationsView.vue
    │   │   ├── ApplyFormView.vue
    │   │   ├── AiMatchView.vue
    │   │   ├── AiMatchResultView.vue
    │   │   ├── AiMatchHistoryView.vue
    │   │   └── AiChatView.vue
    │   └── admin/
    │       ├── DashboardView.vue
    │       ├── UserManageView.vue
    │       ├── BreedManageView.vue
    │       ├── PetManageView.vue
    │       ├── PetFormView.vue
    │       ├── ApplicationListView.vue
    │       └── ApplicationDetailView.vue
    ├── components/
    │   ├── AppHeader.vue            # 公共头部导航
    │   ├── AppFooter.vue            # 公共底部
    │   ├── PetCard.vue              # 宠物卡片
    │   └── FileUpload.vue           # 图片上传组件
    └── utils/
        └── auth.js                  # Token 存取工具函数
```

### 3.3 路由设计

```javascript
const routes = [
  { path: '/', name: 'Home', component: HomeView },
  { path: '/login', name: 'Login', component: LoginView, meta: { guest: true } },
  { path: '/register', name: 'Register', component: RegisterView, meta: { guest: true } },
  { path: '/pets', name: 'PetList', component: PetListView },
  { path: '/pets/:id', name: 'PetDetail', component: PetDetailView },
  // 需要登录
  { path: '/user/profile', meta: { requiresAuth: true } },
  { path: '/user/applications', meta: { requiresAuth: true } },
  { path: '/user/apply/:petId', meta: { requiresAuth: true } },
  { path: '/user/ai-match', meta: { requiresAuth: true } },
  { path: '/user/ai-match/result', meta: { requiresAuth: true } },
  { path: '/user/ai-match/history', meta: { requiresAuth: true } },
  { path: '/user/ai-chat', meta: { requiresAuth: true } },
  // 需要管理员
  { path: '/admin', meta: { requiresAuth: true, requiresAdmin: true } },
  { path: '/admin/users', meta: { requiresAuth: true, requiresAdmin: true } },
  // ... 其他管理路由
  // 404
  { path: '/:pathMatch(.*)*', name: 'NotFound', component: NotFoundView },
]
```

**导航守卫逻辑：**
```javascript
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  if (to.meta.guest && userStore.isLoggedIn) return next('/')
  if (to.meta.requiresAuth && !userStore.isLoggedIn) return next(`/login?redirect=${to.path}`)
  if (to.meta.requiresAdmin && !userStore.isAdmin) return next('/')
  next()
})
```

### 3.4 状态管理

**userStore (Pinia):**
```javascript
export const useUserStore = defineStore('user', () => {
  const token = ref(localStorage.getItem('token') || '')
  const userInfo = ref(null)
  const isLoggedIn = computed(() => !!token.value)
  const isAdmin = computed(() => userInfo.value?.roles?.includes('ROLE_ADMIN'))

  async function login(username, password) { ... }
  async function register(form) { ... }
  function logout() { token.value = ''; userInfo.value = null; localStorage.removeItem('token') }
  async function fetchProfile() { ... }

  return { token, userInfo, isLoggedIn, isAdmin, login, register, logout, fetchProfile }
})
```

### 3.5 Axios 配置

```javascript
// api/request.js
const http = axios.create({
  baseURL: 'http://localhost:8080/api',
  timeout: 10000,
})

// 请求拦截：附加 Token
http.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})

// 响应拦截：统一错误处理
http.interceptors.response.use(
  response => {
    const { code, msg, data } = response.data
    if (code === 200) return data
    ElMessage.error(msg || '请求失败')
    return Promise.reject(new Error(msg))
  },
  error => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      router.push('/login')
    }
    ElMessage.error('网络错误')
    return Promise.reject(error)
  }
)
```

### 3.6 Vite 配置

```javascript
// vite.config.js
export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: { '@': fileURLToPath(new URL('./src', import.meta.url)) }
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
      },
      '/uploads': {
        target: 'http://localhost:8080',
        changeOrigin: true,
      }
    }
  }
})
```

---

## 四、数据库设计

保持现有数据库结构不变（schema.sql），核心表如下：

| 表名 | 说明 | 主要字段 |
|------|------|----------|
| user | 用户表 | id, username, password, email, avatar_url, enabled |
| role | 角色表 | id, name |
| user_role | 用户角色关联 | user_id, role_id |
| pet | 宠物表 | id, name, breed_id, gender, age, status, ... |
| breed | 品种表 | id, breed_name, pet_type, description |
| pet_image | 宠物图片表 | id, pet_id, image_url, is_cover |
| application | 领养申请表 | id, user_id, pet_id, status, reason, comment |
| ai_match_record | AI匹配记录 | id, user_id, preferences, result, created_at |
| ai_review_record | AI审核记录 | id, application_id, suggestion, created_at |

---

## 五、关键技术决策

| 决策项 | 方案 | 原因 |
|--------|------|------|
| 认证方式 | JWT | 无状态，适合 REST API，前后端分离标准方案 |
| UI组件库 | Element Plus | Vue3 生态最成熟，中文文档完善，组件丰富 |
| 构建工具 | Vite | 比 Webpack 快，Vue3 官方推荐 |
| 状态管理 | Pinia | Vue3 官方推荐，API 简洁，TypeScript 友好 |
| 项目组织 | 同仓库 frontend/ 目录 | 课程项目统一管理，方便提交 |
| HTTP库 | Axios | 生态成熟，拦截器机制方便处理 Token |
| 密码加密 | BCrypt | 已在项目中使用的成熟加密算法 |

---

*文档版本：v1.0 | 日期：2026-05-30*
